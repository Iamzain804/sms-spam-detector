name: CI - Test & Build

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  # Job 1 - Backend Test
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Python setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt

      - name: Backend import check
        run: |
          cd backend
          python -c "import pickle, fastapi, uvicorn, nltk, sklearn; print('Backend OK')"

      - name: Check pkl files exist
        run: |
          ls backend/model.pkl && echo "model.pkl OK" || echo "model.pkl missing"
          ls backend/vectorizer.pkl && echo "vectorizer.pkl OK" || echo "vectorizer.pkl missing"

  # Job 2 - Frontend Build
  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Node setup
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install & Build
        run: |
          cd frontend
          npm install
          npm run build

      - name: Build success check
        run: |
          ls frontend/dist
          echo "Frontend build OK"
